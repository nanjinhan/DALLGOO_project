# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
pymysql==1.1.1
cryptography==44.0.0

# --- Cache / Rate limit / 인증번호 (메모리 DB) ---
redis==5.2.1

# --- Message Queue (비동기 이메일 발송) ---
pika==1.3.2

# --- Auth / Security ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1

# --- Config / Validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.0.1

# 참고: passlib 1.7.4 + bcrypt 4.1+ 조합은 버전 인식 경고가 뜨므로
# bcrypt==4.0.1로 고정해 경고 없이 동작시킨다.
